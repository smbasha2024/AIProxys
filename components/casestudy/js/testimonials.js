 // Reinitiliase owlcarousel
 window.initializeTestimonialCarousel();

/*
 // Reinitiliase owlcarousel
 if(`${path}`.includes("testimonials")) {
    //console.log("yes");
    window.initializeTestimonialCarousel();
  }
    */